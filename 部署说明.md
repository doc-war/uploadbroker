# UploadBroker 部署说明

> 自动化部署 + Nginx 反向代理，适用生产环境。

---

## 目录结构

### 本地开发目录

```
项目根目录/
├── uploadBroker.yaml          # 生产配置（git 追踪）
├── deploy/                    # 构建产物目录（gitignore）
│   ├── uploadbroker           # linux/amd64 二进制
│   ├── uploadBroker.yaml      # 从根目录拷贝的配置副本
│   └── deploy.sh              # 自动生成的远端部署脚本
├── deploy.js                  # 自动化部署脚本
└── package.json               # npm 指令入口
```

### 服务器目标目录

```
/opt/uploadbroker/
├── uploadbroker               # 二进制（当前运行版本）
├── uploadBroker.yaml          # 运行时配置
└── data/
    ├── logs/                  # 运行日志
    ├── objects/               # 本地存储文件
    └── broker.db              # SQLite（自动创建）
```

---

## 一、首次部署（手动步骤）

仅首次需要，后续更新走 `npm run deploy` 即可。

### 1.1 安装 Nginx

```bash
ssh wutuo
apt update && apt install -y nginx
```

### 1.2 配置 Nginx 反向代理

创建 `/etc/nginx/sites-available/uploadbroker`：

```nginx
server {
    # Cloudflare 走 HTTP（TLS 由 Cloudflare 终止）
    listen 80;
    server_name uploadbroker.dow-war.com;

    # 只允许 Cloudflare 回源 IP，防止直连绕过
    # 尽管conf.d已经全局配置了cloudflare的ip集合，但这里还是要显示配置，因[>
    include /etc/nginx/conf.d/cloudflare-ips.conf;   # allow 列表，必须在 d>
    deny all;     # 拒绝cloudflare之外的一切ip

    # 资源读取（客户端直接访问 /tmp/...）
    location /tmp/ {
        proxy_pass http://127.0.0.1:8880;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
    }

    # 上传接口（Platform 走 localhost，Nginx 仅做保底）
    location /v1/ {
        proxy_pass http://127.0.0.1:8880;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 12m;
    }

    # 健康检查
    location /v1/health {
        proxy_pass http://127.0.0.1:8880;
        proxy_read_timeout 3s;
        access_log off;
    }
}
```

启用站点：

```bash
ln -s /etc/nginx/sites-available/uploadbroker /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx
```

### 1.3 配置生产配置

编辑项目根目录 `uploadBroker.yaml`，确保以下字段正确：

```yaml
listen: 127.0.0.1:8880
base_url: https://uploadbroker.dow-war.com
url_blake2b_salts:
  - your-production-salt
```

### 1.4 执行首次部署

```bash
npm run deploy -- --force-config
```

`--force-config` 会把本地 `uploadBroker.yaml` 上传到服务器，后续不需要此参数。

### 1.5 验证

```bash
# 检查服务状态
ssh wutuo systemctl status uploadbroker

# 测试健康检查
curl http://uploadbroker.dow-war.com/v1/health
```

---

## 二、日常更新

```bash
npm run deploy
```

脚本自动完成：
1. 运行 `go test ./...`（测试不通过则中断）
2. 交叉编译 linux/amd64 → `deploy/uploadbroker`
3. 拷贝 `uploadBroker.yaml` → `deploy/uploadBroker.yaml`
4. ssh 创建远端目录
5. scp 上传二进制（`.new` 后缀）
6. scp 上传配置（远端不存在时上传，已存在则跳过）
7. systemd 原子替换二进制并重启
8. 检查服务状态

如需强制覆盖远端配置：

```bash
npm run deploy -- --force-config
```

---

## 三、Nginx 运维

### 3.1 查看日志

```bash
# Nginx 访问日志
tail -f /var/log/nginx/access.log

# 应用日志（位于服务器 /opt/uploadbroker/data/logs/）
ssh wutuo tail -f /opt/uploadbroker/data/logs/broker.log
```

### 3.2 修改端口

配置文件中 `listen` 字段控制 Broker 监听端口，修改后需同步 Nginx：

```yaml
# uploadBroker.yaml
listen: 127.0.0.1:8880
```

修改后：

```bash
# 更新远端配置
npm run deploy -- --force-config

# 同步更新 Nginx proxy_pass 端口
ssh wutuo vi /etc/nginx/sites-available/uploadbroker
ssh wutuo systemctl reload nginx
```

---

## 四、架构示意

```
客户端                  Platform
  │                       │
  │                       │ localhost
  ▼                       ▼
HTTPS                  127.0.0.1:8880
Nginx (TLS 终止)       uploadbroker (systemd 守护)
  │                       │
  ▼                       ▼
127.0.0.1:8880        Local / S3 / OSS / R2
```

- Nginx 负责 TLS 终止、域名路由
- Platform 通过 localhost 调用上传接口
- 客户端通过 Nginx 直接读取 /tmp/ 资源
- uploadbroker 仅监听 127.0.0.1，不直接暴露公网
- systemd 负责进程保活、自启动

---

## 五、故障排查

```bash
# 查看服务状态
ssh wutuo systemctl status uploadbroker

# 查看最近日志
ssh wutuo journalctl -u uploadbroker --no-pager -n 30

# 查看应用日志
ssh wutuo tail -50 /opt/uploadbroker/data/logs/broker.log

# 手动重启
ssh wutuo systemctl restart uploadbroker
```
